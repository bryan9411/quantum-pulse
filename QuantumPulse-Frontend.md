# 專案實戰計畫：QuantumPulse Frontend（純前端高頻渲染效能實驗室）

> 本計畫是原 QuantumPulse 全端計畫的**前端專精改版**。核心改動：拿掉「先做後端才做前端」的順序，
> 讓你**第一天就能在瀏覽器裡看到資料流動、親手調速度、親眼看到掉幀**，而不是盲飛一整個階段才第一次看到畫面。
> 定位維持：**我是前端工程師，這份計畫不要求你寫任何伺服器程式碼。**

---

## 這一版跟原本 QuantumPulse 差在哪裡

| 項目 | 原版 QuantumPulse | 這一版 Frontend 版 |
| :--- | :--- | :--- |
| 資料來源 | 自架 Node.js 後端 + 幣安上游連線 | **瀏覽器內建的 Mock 資料產生器**（Web Worker 裡跑，純前端） |
| 學習順序 | 後端引擎 → 後端整合 → 前端才開始 | **前端從第一天開始**，資料源到最後才「可選」換成真實來源 |
| 回饋迴圈 | 要等整個階段 1 做完才看得到畫面（天級） | 改一行參數，幾秒內就能看到掉幀變化（分鐘級） |
| 教學法 | 黃金四層，直接給答案 | **黃金五層**，第五層由 AI **當下動態出練習題**，不是文件裡寫死的固定題庫 |
| 是否需要碰後端 | 需要 | **完全不需要** |
| 版本控制 | 沒有規劃 | AI **每個小階段結束時判斷是否需要 commit**，提供 commit 訊息建議，最終是否 commit 由使用者決定 |

---

## 🚀 跨裝置「一鍵喚醒無縫接續」通關密語

> [!TIP]
> 開新對話時，貼上這句話即可無縫接續：
>
> **「請閱讀專案根目錄的 `QuantumPulse-Frontend-plan.md`，嚴格遵循其中的【黃金五層教學法】與協作守則，
> 查看目前打勾進度，我們目前在【階段 X-X】，請手把手繼續帶我進行！」**
>
> AI 讀取本文件後務必記得：**練習題與是否需要 commit，都由 AI 在教學當下依該階段的複雜度自行判斷產生，
> 不是照抄文件裡的固定清單（文件裡本來就沒有寫死題目與 commit 序列）。**

---

## 視覺成果目標（維持 Apple / Vercel 白晝 FinTech 美學，走簡潔質感路線、避免過度花俏的「AI 生成感」）

- 主色調以白 / 灰階為主，紅綠僅用在買賣、漲跌等語意明確的地方，不濫用漸層與強烈色塊
- 右上角膠囊按鈕 `[ ⚡ 極致加速 | 基準對照 ]`，用細邊框、不用厚重陰影
- 數字類資訊（價格、TPS、FPS）建議用等寬字體（monospace），閱讀起來更像真正的交易終端機
- 效能監控區塊（FPS / TPS / JS Heap / 緩衝佔用率）改用**小型走勢圖（sparkline）搭配數字**，比純數字卡片更直覺、也更耐看

---

## 嚴格教學與協作守則（黃金五層教學法）

> [!IMPORTANT]
> 第零層是整份計畫最關鍵的改動：逼你先撞牆、先猜錯，才有資格看答案。

### 第零層：自主碰撞與假設驗證（Break-First Learning）
1. 先寫最笨、最沒優化的版本。
2. 親自製造壓力測試場景（拉高 TPS、CPU throttling）。
3. 自己先寫下猜想（畫面卡頓/記憶體飆升的原因）。
4. 用 DevTools 驗證猜想對不對，不是用讀答案驗證。

### 第一層：生活情境比喻
### 第二層：前後端/上游互動流程圖（ASCII）
### 第三層：程式碼分塊手把手組裝（積木拼裝法）
- 變數與狀態零死角解構、微觀因果數字化解析、資深面試亮點、白話解讀、關鍵原理直擊
### 第四層：完整代碼合體與驗證

### 第五層：練習題自測與批改關卡（規則層，題目由 AI 當下動態產生，此文件不預先寫死任何題目）
- **每個小階段的第一到第四層教學結束後，AI 必須自行判斷該階段是否需要出練習題**：
  - 如果該階段概念複雜、容易搞混（例如牽涉數學公式、架構設計取捨、邊界案例），AI 應該出**多題**，題目要能檢驗「理解」而不是「記憶」（例如故意問變形情境、故意問「如果拿掉某個機制會怎樣」）。
  - 如果該階段單純是環境建置、重複性整合、或純粹體力性的組裝步驟，AI 應該直接說明「這階段不需要練習題」，不強行湊題目。
  - 題目數量沒有固定上限或下限，完全由 AI 依內容複雜度判斷。
- **AI 出的練習題一律不附答案**。
- **使用者作答後，AI 負責批改**：明確指出答對的部分、答錯或不完整的部分，並詳細講解正確的理解應該是什麼。
- **批改完成、使用者表示理解後，才可以打勾進入下一個小階段**。不能因為使用者想加快進度就跳過批改直接往下教。

---

## 工程規範與美學
- 全專案 2 個空格縮排，禁止 Tab。
- 路由/工具函式用純函式；長駐有狀態基礎設施（Worker 管理器、Ring Buffer）用 Class 封裝。
- 保留 Naive（未優化）與 Optimized（優化後）雙模式，錄製 Chrome DevTools 火焰圖做真實 A/B 對照。

---

## 版本控制策略（規則層，commit 時機由 AI 當下判斷，此文件不預先寫死固定 commit 序列）

> 目的：讓 commit history 看起來像真實的個人專案演進過程（先寫爛版本 → 親眼看到問題 → 修正），
> 而不是複製教學文件之後一次貼上去。

### 運作方式
- **每完成一個有意義的行為改變（例如：寫出一個會壞掉的笨版本、觀察到一個效能問題、做完一次優化、補上一組測試），AI 要主動判斷這是否構成一次值得 commit 的節點。**
- **如果 AI 判斷需要 commit**：主動提供一則建議的 commit message（採用 Conventional Commits 格式：`feat:` `fix:` `perf:` `test:` `docs:` `chore:` `wip:`），並簡短說明「為什麼建議在這個時間點 commit」。
- **如果 AI 判斷這個步驟太瑣碎、不構成獨立的行為改變**：明確回覆使用者「這裡不需要 commit」，不要為了湊次數硬生出 commit 訊息。
- **是否真的執行 `git commit`，最終由使用者自己決定**——AI 只負責判斷時機與提供訊息建議，不代替使用者做這個決定，也不假設使用者一定會照做。

### Commit message 撰寫原則（給 AI 產生訊息時參考）
1. 採用 Conventional Commits 格式，subject 簡短一行講清楚做了什麼。
2. **第零層碰撞的笨版本，commit message 要誠實標註這是刻意先做的未優化版本**，例如：`wip: naive setInterval 直接 setState，尚未優化，先觀察卡頓`。
3. **觀察到問題時，可以用 `docs:` 或 `wip:` 記錄觀察結果本身**，不是只記錄程式碼變更，例如：`docs: 記錄 2000 TPS 下的卡頓觀察 — FPS 掉到 12，主執行緒 Long Task 佔滿`。
4. **修正/優化的 commit，要在 message 裡說明「為什麼」，不是只說「改了什麼」**，例如：`perf: 導入 Web Worker 分流，避免 Mock 資料產生佔用主執行緒`。
5. 避免無資訊量的訊息，例如 `update`、`fix bug`。

---

## 假資料生成方法說明（此段落要放進 README，避免被誤認為真實資料）

> 這段文字你可以直接改一改貼到專案 README 的「資料來源說明」章節。

**這個系統的行情資料不是寫死的固定資料集，也不是連線真實交易所，而是在瀏覽器 Web Worker 裡即時運算生成的模擬資料**，方法如下：

1. **價格模擬：幾何布朗運動（Geometric Brownian Motion, GBM）**
   每一個時間點，用以下公式從「目前價格」算出「下一個價格」：
   ```
   下一價格 = 目前價格 × exp((drift − 0.5 × volatility²) × dt + volatility × √dt × Z)
   ```
   - `drift`：長期趨勢是漲是跌
   - `volatility`：震盪劇烈程度
   - `Z`：一個標準常態分布的隨機變數（不能直接用 `Math.random()`，因為那是均勻分布；需要用 **Box-Muller 轉換**把兩個均勻分布的亂數轉成常態分布，這樣模擬出來的價格才會有「真實股價的延續性」，而不是每個點都亂跳）
2. **盤口深度模擬**：以模擬出的中間價為基準，往上下兩側生成遞減或隨機分布的掛單量，模擬買賣雙方排隊掛單的樣子。
3. **（建議加值項）歷史真實資料回放模式**：除了純數學模擬，額外提供「錄製一段真實幣安歷史 tick 資料（存成靜態 JSON），前端用可調速度回放」的模式，讓視覺化邏輯可以拿真實市場數據驗證過，同時仍然 100% 是前端範疇（純粹讀取靜態檔案 + 自己控制播放速度），不需要任何即時網路連線或後端維運。

### README 建議段落範本
> **⚠️ 資料來源說明**：本專案展示的行情資料為前端模擬生成（採用幾何布朗運動模型），或為預先錄製的歷史資料回放，**並非即時真實交易所報價**，僅用於展示高頻資料渲染的效能處理能力。

---

## 關鍵術語科普

| 專有名詞 | 英文對應 | 白話解釋 | 面試為什麼要這樣用？ |
| :--- | :--- | :--- | :--- |
| **資料源抽象化** | Data Source Abstraction | 把「資料從哪裡來」跟「畫面怎麼渲染」拆成兩個互不相干的模組。 | 證明你懂依賴反轉，換資料源不用動渲染層。 |
| **前端側背壓** | Client-side Backpressure | 資料到達速度超過渲染能力時，前端自己決定丟棄、合併還是排隊。 | 多數前端工程師只知道後端背壓，這是稀缺知識點。 |
| **合併策略（Coalescing）** | State Coalescing | 短時間內多筆更新只保留最新一筆。 | 展現 UX 判斷力：使用者只在乎看到的畫面。 |
| **零拷貝轉移** | Transferable Objects | Worker 與主執行緒傳遞 ArrayBuffer 時轉移所有權，不複製。 | 證明懂瀏覽器記憶體模型。 |
| **緩衝佔用率** | Buffer Watermark | Ring Buffer 目前裝了多少未消費的資料量。 | 展現主動監控系統健康度的習慣。 |
| **記憶體洩漏偵測** | Memory Leak Detection | 反覆拍 Heap Snapshot 比對物件數量增長。 | 證明關心「跑很久之後」的系統行為。 |
| **幾何布朗運動** | Geometric Brownian Motion | 一種模擬價格隨機波動但具備延續性的數學模型。 | 證明有基本量化/數學建模能力，在 FinTech 前端加分。 |

---

## 系統架構

```
quantumpulse-frontend/
├── app/
├── components/
├── workers/
│   ├── mock-generator.worker.ts
│   └── stream-processor.worker.ts
├── lib/
│   ├── data-source.ts
│   ├── ring-buffer.ts
│   ├── coalescing-strategies.ts
│   └── types.ts
├── hooks/
│   ├── useWorkerStream.ts
│   └── usePerformanceHUD.ts
├── __tests__/
├── package.json
└── QuantumPulse-Frontend-plan.md
```

---

## 技術選型
* **核心框架**：`next` (v15+)、`react` (v19)、`react-dom`
* **樣式與圖標**：`tailwindcss`、`lucide-react`
* **圖表管線**：`lightweight-charts`
* **DOM 虛擬化**：`@tanstack/react-virtual`
* **資料校驗**：`zod`
* **測試**：`jest`、`@testing-library/react`

---

## 階段性實作詳細規劃

> 練習題與 commit 時機不寫在下面的清單裡，由 AI 在每個小階段教學當下依【黃金五層教學法】與【版本控制策略】自行判斷產生。

### 🏁 階段 0：專案初始化與共享型別
- [x] **0-1. Next.js 15 專案建立**
- [ ] **0-2. 型別定義**：`lib/types.ts`（`TradeTick`、`OrderBookDepth`、`TelemetryMetrics`、`DataSourceEvent`）
- [ ] **0-3. 資料源抽象介面設計**：`lib/data-source.ts`，定義 `DataSource` interface，之後 Mock 與真實資料源都實作這個介面

---

### ⚙️ 階段 1：建立「假水龍頭」Mock 資料產生器
- [ ] **1-A. 第零層碰撞**：先用最笨的 `setInterval` 產生假資料觀察手感
- [ ] **1-B. 幾何布朗隨機漫步價格模擬**：`workers/mock-generator.worker.ts`
- [ ] **1-C. 微結構盤口生成**
- [ ] **1-D. 動態 TPS 控制介面**
- [ ] **1-E. Mock 實作 `DataSource` 介面**

---

### 📉 階段 2：Naive Pipeline 建置與親眼觀察崩潰
- [ ] **2-A. 第零層碰撞（強制）**：主執行緒直接 `setState`，不做優化，觀察崩潰
- [ ] **2-B. Chrome DevTools 火焰圖實錄**
- [ ] **2-C. React Reconciliation 成本分析**

---

### ⚡ 階段 3：Web Worker 零拷貝分流改造
- [ ] **3-A. 第零層碰撞**：比較 `postMessage` 與 `Transferable Objects` 的耗時差異
- [ ] **3-B. 建立串流背景處理 Worker**
- [ ] **3-C. Transferable Objects 記憶體零拷貝轉移**
- [ ] **3-D. 主執行緒與 Worker 雙向通訊橋接**

---

### 🔄 階段 4：Ring Buffer + RAF 幀排程 + 前端側背壓策略
- [ ] **4-A. 第零層碰撞**：`Array.push/shift` 的記憶體死穴親眼觀察
- [ ] **4-B. TypedArray 環形緩衝區實作**
- [ ] **4-C. requestAnimationFrame 60FPS 幀率排程器**
- [ ] **4-D. 緩衝佔用率即時監控**
- [ ] **4-E. 前端側背壓策略設計與變形實驗**：丟棄 / 合併 / 佇列 三種策略比較

---

### 🎨 階段 5：極致渲染層
- [ ] **5-A. Apple / Vercel 極簡現代白晝設計系統**
- [ ] **5-B. lightweight-charts Canvas 圖表管線**
- [ ] **5-C. 高資訊密度深度盤口階梯元件**
- [ ] **5-D. 成交流水虛擬化滾動列表**
- [ ] **5-E. 導航列與膠囊控制器**

---

### 🏆 階段 6：測試補強 + Performance HUD + 長時間壓測
- [ ] **6-A. 右上角毛玻璃 A/B 切換膠囊按鈕**
- [ ] **6-B. 即時硬體遙測儀表板（Performance HUD）**
- [ ] **6-C. 動態 TPS 壓測調頻滑桿**
- [ ] **6-D. 單元測試（Jest + Testing Library）**：Ring Buffer、合併邏輯、Worker 斷線重連
- [ ] **6-E. 長時間壓測與記憶體洩漏偵測**
- [ ] **6-F. 低階裝置模擬測試**：CPU/Network throttling
- [ ] **6-G. 終極成果驗證與面試 STAR 說話術總驗收**

---

### 🌐 階段 7（完全選修，不影響前端定位）：接上真實資料源
- [ ] **7-A. 幣安公開 WebSocket API 直連**：不需要自己架伺服器，純前端多實作一個 `DataSource`
- [ ] **7-B. 驗證抽象層價值**：把資料源從 Mock 換成真實連線，UI 元件完全不用改
- [ ] **7-C.（可選）錯誤分級處理**：斷線重連 / 限流警告 / 資料格式異常，三種 UI 提示邏輯設計
